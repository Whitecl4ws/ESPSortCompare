#ifndef HEADER_ALGORITHMS
#define HEADER_ALGORITHMS
#include "defs.h"
int getRandomIntegerALG(unsigned range);
void surfacesSwap(struct Surface * a, struct Surface * b);
float surfacesGetMaximumIndex(struct Surface * array, int n);
#endif
